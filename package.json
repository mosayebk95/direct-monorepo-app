{
  "name": "direct-monorepo-app",
  "version": "0.0.1",
  "description": "A professional React monorepo setup with shared UI components",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "dev:web": "pnpm --filter web dev",
    "build:packages": "pnpm --filter ui build && pnpm --filter utils build",
    "build": "pnpm build:packages && pnpm --filter web build",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\""
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  },
  "author": "mosayeb.kia95@gmail.com",
  "license": "ISC",
  "packageManager": "pnpm@10.26.0"
}
